# dct_config.py
# ==================================================
# DCT 全局配置（主线 + 消融扩展）
# ==================================================

DCT_CFG = {
    # -------------------------
    # 主线：2D-DCT（不动）
    # -------------------------
    "gpr": {
        "Kx": 32,
        "Ky": 32,
        "norm": "ortho"
    },
    "ert": {
        "Kx": 32,
        "Ky": 32,
        "norm": "ortho"
    },

    # -------------------------
    # [MOD] 消融：ERT 分块 DCT + 位置编码
    # -------------------------
    "ert_block": {
        # 分块参数
        "block_size": 16,     # 每块尺寸（16x16）
        "stride": 16,         # 步长（无重叠，最稳）
        "keep": 8,            # 每块保留 8x8 低频

        # 位置编码参数
        "pos_enc": "2d_sincos",
        "pos_dim": 16,        # 位置编码维度（x/y 各 8）

        # 聚合方式
        # concat: 保留空间分布（论文推荐）
        # mean  : 强压缩（可选消融）
        #"agg": "concat"#2d
        "agg": "mean"#block
    }
}
